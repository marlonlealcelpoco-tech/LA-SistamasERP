# LA Cotações Android

Aplicativo Android compartilhado para produtos, fornecedores e cotações.

## API

Defina `VITE_API_URL` no build ou use o padrão para emulador Android (`http://10.0.2.2:3333`).

O aplicativo usa `/auth/login` e os endpoints `/quotations/*` do backend.
