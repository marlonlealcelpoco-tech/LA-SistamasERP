import type { CapacitorConfig } from "@capacitor/cli";
const config: CapacitorConfig = { appId:"br.com.lasistemas.cotacoes", appName:"LA Cotações", webDir:"dist", server:{ cleartext:true } };
export default config;
